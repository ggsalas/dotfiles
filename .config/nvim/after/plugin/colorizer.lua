-- duplicate this config here to avoid an error message
-- the original is on "after/plugins/colors.lua"
vim.o.termguicolors = true

require('nvim-highlight-colors').setup {}

-- require 'colorizer'.setup {
--   filetypes = {
--     css = {always_update = true}
--   },
-- }

